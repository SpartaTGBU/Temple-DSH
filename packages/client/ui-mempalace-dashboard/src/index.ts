/**
 * MemPalace dashboard browser plugin, node half. The Host API lives in
 * `@deepseek-ai/dsh-api-mempalace-dashboard`; this package only contributes
 * the optional Settings section when composed in the client roster.
 */

/** Host plugin body — this package contributes browser presentation only. */
export function apply(): void {}
